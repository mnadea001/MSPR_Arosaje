<form method="post" action="{{ path('app_plant_sitting_delete', {'id': plant_sitting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plant_sitting.id) }}">
    <button class="btn">Delete</button>
</form>
